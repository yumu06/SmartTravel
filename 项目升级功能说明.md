# 随心游项目升级功能说明

更新日期：2026-08-25

## 1. 升级目标

本次升级保留原有的旅行路线规划、文章社区、评论点赞、收藏、旅行足迹、聊天和公告等业务，重点完善以下问题：

- 登录凭证生命周期与注销机制不完整；
- 数据库连接、慢查询和核心索引缺少统一配置；
- 推荐查询存在全表扫描和 filesort；
- 小程序 AppID、入口文件及接口地址配置不统一；
- 数据库、Redis和第三方服务密钥存在明文配置风险；
- 缺少隔离的压测数据、压测工具和可量化的性能基线；
- 自动化测试和构建验证不足。

## 2. 已完成升级

### 2.1 Access Token + Refresh Token 认证体系

后端认证由单一 Token 升级为双 Token：

- Access Token 有效期默认为 15 分钟，用于访问受保护业务接口；
- Refresh Token 有效期默认为 7 天，只用于换取新 Token；
- JWT 使用独立的 Access Secret 和 Refresh Secret；
- Token 包含用户 ID、Session ID、Token 类型、JTI、签发时间和过期时间；
- `/travel/auth/refresh` 提供刷新接口；
- `/travel/auth/logout` 提供主动注销接口。

Redis 保存 Refresh Session，并通过 Lua 脚本原子校验和轮换 Refresh JTI。旧 Refresh Token 被重复使用时会识别为重放行为并删除对应会话。注销时删除 Refresh Session，同时将尚未过期的 Access Token JTI 写入黑名单。

> 当前状态：后端双 Token 能力已经完成；小程序端目前主要保存和使用 Access Token，尚未实现“收到 401 后自动刷新并重试原请求”的完整流程。

### 2.2 MySQL连接池与慢查询配置

数据库层增加以下配置：

- 最大打开连接数；
- 最大空闲连接数；
- 连接最大生命周期；
- 空闲连接最大生命周期；
- 慢 SQL 阈值及日志输出；
- 数据库初始化和迁移错误检查。

本地默认配置为 50 个最大连接、10 个空闲连接和 200ms 慢 SQL 阈值，可通过环境变量覆盖。

### 2.3 核心业务索引优化

为用户、文章、评论、足迹、聊天、点赞和收藏关系补充唯一索引或组合索引，主要包括：

- 用户 OpenID 唯一索引；
- 用户与文章的点赞、收藏唯一关系索引；
- 文章作者与发布时间索引；
- 评论文章与发布时间索引；
- 足迹用户与发布时间索引；
- 聊天双方与发布时间索引；
- 推荐排序复合索引。

推荐接口使用以下排序索引：

```text
idx_post_recommend(like_count DESC, view_count DESC, created_at DESC)
```

优化前推荐 SQL 需要扫描约 9,808 行并执行 filesort；优化后使用复合索引，预计扫描约 39 行，不再执行 filesort。

### 2.4 推荐接口性能优化结果

在本机、10,000 篇基准文章、200 并发条件下：

| 指标 | 优化前 | 优化后 | 效果 |
| --- | ---: | ---: | ---: |
| 成功 QPS | 471.27 | 7,020.93 | 约 14.9 倍 |
| P95 | 1,144.69ms | 43.41ms | 降低约 96.2% |
| P99 | 1,482.10ms | 82.37ms | 降低约 94.4% |
| 错误率 | 0% | 0% | 保持为 0 |

完整数据见 [`travel/benchmark/2026-08-22-core-business-baseline.md`](travel/benchmark/2026-08-22-core-business-baseline.md)。

### 2.5 独立压测环境

新增 `travel_benchmark` 专用数据库，不对现有 `travel_database` 执行删除、清空或灌数操作。基准数据包括：

| 数据类型 | 数量 |
| --- | ---: |
| 用户 | 1,000 |
| 文章 | 10,000 |
| 评论 | 50,000 |
| 点赞关系 | 100,000 |
| 收藏关系 | 30,000 |
| 旅行足迹 | 10,000 |
| 合计 | 201,000 |

数据生成器支持固定规模、确定性 UUID、批量插入、数据库名称安全校验和最终数量检查。

### 2.6 Go 压测工具

新增 Go 并发压测工具，支持：

- Bearer Token 请求头；
- 单 URL 或多 URL 轮询；
- 自定义并发数、请求数和超时时间；
- 成功 QPS、错误率、P50、P95、P99和最大延迟；
- HTTP 状态、网络错误和超时分类；
- 从环境变量读取 Access Token，不在输出中显示凭证。

文章详情压测轮询 100 个文章 ID，避免只测试单一热点数据。

### 2.7 小程序工程配置

小程序端完成以下配置修正：

- 清除仓库中的个人 AppID，保留空配置项并在本地或微信开发者工具中填写；
- 补充根目录 `app.js`，恢复登录状态和全局数据；
- 在 `miniprogram-5/config/index.js` 统一维护后端 API 根地址；
- 移除登录组件和用户页面中重复硬编码的接口地址；
- 更新微信开发者工具中的项目名称和项目描述。

开发者工具可使用 `http://127.0.0.1:1016`。真机调试应改为电脑局域网地址；正式发布必须使用已在微信公众平台配置的 HTTPS 合法域名。

### 2.8 配置安全

以下敏感项不再保存在 `application.yml` 中，统一通过环境变量注入：

- MySQL 密码；
- Redis 密码；
- JWT Access/Refresh Secret；
- 微信 AppID；
- 微信 AppSecret；
- 腾讯地图 WebService Key；
- 管理员 Token。

项目提供 `travel/config/environment.example.ps1` 作为本地模板，实际配置文件 `environment.local.ps1` 已加入忽略规则。

### 2.9 自动化测试与验证

已补充或完善以下测试：

- JWT 签发、解析、Token 类型和密钥校验；
- Refresh Token 原子轮换和重放检测；
- Redis Session、黑名单和过期时间；
- MySQL 连接池参数；
- 推荐复合索引字段及顺序；
- 配置文件环境变量覆盖；
- 压测工具 URL 轮询、Authorization 和错误统计；
- 基准数据数量和唯一关系校验。

当前代码已通过：

```text
go test -count=1 ./...
go vet ./...
go build ./...
```

## 3. 当前核心接口压测基线

以下为本机 200 并发测试结果：

| 接口 | 成功 QPS | P95 | 错误率 |
| --- | ---: | ---: | ---: |
| 鉴权 | 13,928.81 | 17.73ms | 0% |
| 用户信息 | 9,301.24 | 29.47ms | 0% |
| 文章详情 | 5,347.03 | 62.45ms | 0% |
| 推荐（索引优化后） | 7,020.93 | 43.41ms | 0% |

这些结果用于本地版本间对比，不等同于生产服务器容量承诺。

## 4. 尚未完成或后续计划

以下内容不属于当前已交付功能：

1. 小程序端自动使用 Refresh Token、并发刷新合并和原请求重试；
2. 鉴权用户信息 Redis 缓存的生产接入与缓存失效策略；
3. 文章浏览量通过 Redis 聚合并批量回写 MySQL；
4. 推荐结果短 TTL 缓存及更新失效机制；
5. Docker、Kubernetes、RabbitMQ 和 Elasticsearch 部署或中间件接入；
6. 生产环境 HTTPS 域名、证书、监控和告警配置。

简历或项目介绍中应将上述内容描述为后续计划，不能写成已经完成。

## 5. 主要升级文件

| 文件或目录 | 说明 |
| --- | --- |
| `travel/auth/` | 双 Token、Redis Session、黑名单和重放检测 |
| `travel/pkg/jwt/` | JWT 签发、解析与轮换 |
| `travel/TravelDate/tavelDate.go` | 数据库连接池、慢 SQL 和迁移 |
| `travel/TravelModel/` | 模型索引配置 |
| `travel/benchmark/` | 压测工具、数据生成和报告 |
| `travel/cmd/benchmark-setup/` | 独立压测数据库初始化命令 |
| `travel/cmd/benchmark-token/` | 压测 Token 和 Redis Session 创建命令 |
| `travel/config/application.yml` | 非敏感默认配置 |
| `travel/config/environment.example.ps1` | 本地环境变量模板 |
| `miniprogram-5/config/index.js` | 小程序 API 地址配置 |
| `miniprogram-5/project.config.json` | 微信开发者工具项目配置 |
